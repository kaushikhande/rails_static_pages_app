<% provide(:title,@user.name) %>
<div class="row">
  <div class="col-md-4">
    
      <div class="aside">
        <section class="user_info">
          <h1>
            <%= gravatar_for @user %> Hi <%= @user.name %> !!
          </h1>
        </section>
      </div>
  </div>
</div>
